% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monty-hall-pkg.R
\name{determine_winner}
\alias{determine_winner}
\title{Determine if the contestant won the game.}
\usage{
determine_winner(final.pick, game)
}
\arguments{
\item{final.pick}{A single integer (1, 2, or 3) representing the index of
the contestant's final door choice (e.g., generated by \code{change_door()}).}

\item{game}{A character vector of length 3 representing the game board setup
(e.g., generated by \code{create_game()}).}
}
\value{
The function returns a single character string: either \code{"WIN"} if the
final choice hides a car, or \code{"LOSE"} if it hides a goat.
}
\description{
\code{determine_winner()} evaluates whether the contestant's final door choice
contains the car ("WIN") or a goat ("LOSE").
}
\details{
This function serves as the conclusion of the Monty Hall simulation. It maps
the contestant's final selected door index back to the underlying game
setup vector to check if the final choice matched the winning prize.
}
\examples{
  this_game <- create_game()
  my_pick <- select_door()
  goat_door <- open_goat_door( game = this_game, a.pick = my_pick )
  final_pick <- change_door( stay = FALSE, opened.door = goat_door, a.pick = my_pick )

  determine_winner( final.pick = final_pick, game = this_game )

}
